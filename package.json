{
  "name": "livia",
  "version": "6.0.0",
  "description": "Livia \u2014 self-hosted, white-label AI Executive Assistant",
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "start": "node --max-old-space-size=460 server.js",
    "check-env": "node -e \"const r=['ANTHROPIC_API_KEY','GOOGLE_CLIENT_ID','GOOGLE_CLIENT_SECRET','GOOGLE_REFRESH_TOKEN','OWNER_EMAILS','OWNER_EMAIL','OWNER_NAME','LIVIA_EMAIL'];const m=r.filter(k=>!process.env[k]);if(m.length){console.error('Missing env vars:',m.join(', '));process.exit(1);}console.log('All required env vars present.');\"",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0",
    "@googleapis/calendar": "^9.8.0",
    "@googleapis/gmail": "^12.0.0",
    "@googleapis/people": "^3.0.9",
    "cors": "^2.8.5",
    "docx": "^9.0.0",
    "dotenv": "^17.3.1",
    "express": "^4.21.2",
    "google-auth-library": "^9.15.1",
    "pdf-parse": "^1.1.1"
  }
}
